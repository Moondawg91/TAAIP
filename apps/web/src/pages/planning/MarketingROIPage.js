import React from 'react'
import { Box, Typography } from '@mui/material'

export default function MarketingROIPage(){
  return (
    <Box sx={{ p:3 }}>
      <Typography variant="h4">Marketing & Ad ROI</Typography>
      <Typography sx={{ mt:2, color:'text.secondary' }}>Marketing and advertising return on investment reports.</Typography>
    </Box>
  )
}
